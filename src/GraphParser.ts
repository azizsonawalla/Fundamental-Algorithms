/**
 * Reads a graph input from a text file and returns an adjacency matrix
 * @param path path to the text file
 */
export function read(path: string): Array<Array<number>> {
    throw new Error("Not Implemented")
}